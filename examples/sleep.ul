assign(delay_print, (function(elements, delay) {
    console.clear();
	dolist(i,elements)
	{
		console.println(i);
		sleep(delay);
	}
}));

name = ["Hello", "World"];
delay_print(["Hello", "World", (name[0]), "How are", "you?"], 500);