// Hello World

assign(name, [ "Hello", "World" ]);

console.println(name[1]);
assign(greet, (function (name, age) // Comment Test
{	
	console.println((string.format("Hello ~A, who is ~A years old.", name, age)));
}));

greet("Josh", (num(15,0)), 0);
greet("Bill", -12.34);